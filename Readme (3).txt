Hi sir i hope you are doing well, i ceate this accessment/test using React js (MUI), React Rouster dom(v6), React table(v8), 
You can run this by simple typing ( npm i ) in terminal to install node-modules.
Local host that You can run this by simple typing ( npx json-server --watch db.json --port 3333 ) in terminal.

 